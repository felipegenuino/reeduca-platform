# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
# Chave anon/public — exposta no front; use apenas para leitura pública. Nunca coloque service_role aqui.
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key-aqui
# Chave service_role (apenas servidor; necessária para /admin). NUNCA commitar nem expor no front.
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# Asaas (Pagamentos) — não commitar valores reais
ASAAS_API_KEY=sua-api-key-asaas
ASAAS_WEBHOOK_SECRET=seu-webhook-secret

# Site URL (localhost em dev; URL do app em produção)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
